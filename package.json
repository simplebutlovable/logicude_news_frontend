{
  "name": "example-create-react-app-express",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn build",
    "server": "cd functions && node server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
    "build": "cd client && npm instal && npm run build"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "html-to-text": "^7.1.1",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
